import React from 'react';

import css from './PageNotFound.module.css'

const PageNotFound = () => {
    return (
        <div className={css.wrap}>
            <span className={css.text_style}>PAGE NOT FOUND</span>
        </div>
    );
};

export default PageNotFound;