import React from 'react';

import {Header, MovieList} from "../../components";

const MoviesPage = () => {

    return (
        <div>
            <Header/>
            <MovieList/>
        </div>
    );
};

export default MoviesPage;